apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    {{- include "oht-devops.labels" . | nindent 4 }}
  name: {{ .Values.kibana.name }}-pv
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: "{{ .Values.kibana.persistentVolume.storageClass }}"
  resources:
    requests:
      storage: "{{ .Values.kibana.persistentVolume.size }}"

